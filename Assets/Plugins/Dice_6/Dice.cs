using UnityEngine;

public class Dice : MonoBehaviour
{
    public enum DiceSide
    {
        One = 1,
        Two = 2,
        Three = 3,
        Four = 4,
        Five = 5,
        Six = 6,
    }

    // Update is called once per frame
    void Update() { }
}
